<% if @comment.persisted? %>
	$('#<%= comments_container_id(@comment.commentable) %>')
		.append("<%= j render partial: 'shared/comment', object: @comment %>");
	resetCommentForm("<%= comment_form_id(@comment.commentable) %>");
	setCommentsCounters("<%= pluralize(@comment.commentable.comments.count, 'Comment') %>");
<% end %>
