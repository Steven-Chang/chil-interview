<% if policy(:post).create? %>
  <%= link_to(t("new.text", resource: "Post"), new_post_path, class: "form__button form__button--primary") %>
<% end %>

<%= render partial: "shared/post", collection: @posts, locals: { link_title: true, post_border: true } %>
