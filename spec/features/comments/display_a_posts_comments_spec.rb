# frozen_string_literal: true

require "rails_helper"

RSpec.describe "comments", type: :feature, js: true do
	let!(:post) { create(:post) }

	context "when commentable is comment"

	context "when commentable is post"
end
